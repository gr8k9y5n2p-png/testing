services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///./data/distributions.db
      FETCH_MODE: fixture
    volumes:
      - dist-data:/app/data

volumes:
  dist-data:
